{
  traces: [{
    x: {{json(array(persons).extract("age"))}},
    y: {{json(array(persons).extract("salary"))}},
    mode: "markers",
    type: "scatter"
  }],
  layout: {
    plot_bgcolor: "rgba(255,255,255,1)"
  }
}
