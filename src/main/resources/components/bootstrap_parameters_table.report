component("bootstrap_parameters_table") {
    table(class: "table table-sm") {
        tbody {
            options.parameters.each { key, value ->
                tr(class: "small") {
                    td key
                    td value
                }
            }
        }
    }
}
